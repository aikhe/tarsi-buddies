export type SiteContent = {
  title: string;
  description: string;
};

// central content model (mirrors the data-driven pattern in ego-ipse).
export const siteContent: SiteContent = {
  title: 'Tarsi Buddies!',
  description: 'Coming soon.',
};
